# HTTP 服务演示场景

本场景演示如何将本地 HTTP 服务通过 gotunnel 暴露到公网。

## 🎯 演示目标

- 在本地启动一个 HTTP 服务（端口 8080）
- 通过 gotunnel 将服务映射到公网端口 18080
- 通过公网 IP 访问本地 HTTP 服务

## 📋 前置准备

1. **服务端**：有公网 IP 的云服务器
2. **客户端**：本地内网机器
3. **本地 HTTP 服务**：需要有一个运行在 8080 端口的 HTTP 服务

## 🚀 演示步骤

### 步骤 1：准备本地 HTTP 服务

在客户端机器上启动一个简单的 HTTP 服务：

```bash
# 方式1：使用 Python（推荐）
python3 -m http.server 8080

# 方式2：使用 Node.js
npx http-server -p 8080

# 方式3：使用 Go
go run -m http.server 8080
```

验证服务是否启动：

```bash
curl http://127.0.0.1:8080
```

### 步骤 2：配置服务端

将 `server.yaml` 复制到项目根目录：

```bash
cp example/http/server.yaml config.yaml
```

修改配置（如需要）：

```yaml
server:
  addr: "0.0.0.0:17000"     # 监听所有网卡
  log_level: "info"
  log_lang: "zh"
  token: "http-demo-token"  # 自定义 token
```

### 步骤 3：配置客户端

将 `client.yaml` 复制到项目根目录（覆盖之前的配置）：

```bash
cp example/http/client.yaml config.yaml
```

**重要**：修改 `server_addr` 为实际的服务端公网 IP：

```yaml
client:
  server_addr: "YOUR_SERVER_IP:17000"  # 替换为实际 IP
  local_ports: [8080]                  # 本地 HTTP 服务端口
  remote_port: 18080                   # 公网暴露端口
  token: "http-demo-token"            # 必须与服务端一致
```

### 步骤 4：启动服务端

在服务端机器上：

```bash
# 编译（如果还没有）
go build -o gotunnel-server ./cmd/server

# 启动服务端
./gotunnel-server
```

看到以下日志表示启动成功：

```
[INFO][server] 控制通道监听中: 0.0.0.0:17000, Token: http-demo-token
```

### 步骤 5：启动客户端

在客户端机器上：

```bash
# 编译（如果还没有）
go build -o gotunnel-client ./cmd/client

# 启动客户端
./gotunnel-client
```

看到以下日志表示连接成功：

```
[INFO][client] 端口已注册: 8080 -> 18080
[INFO][client] 端口注册成功
```

### 步骤 6：验证演示

在浏览器或使用 curl 访问：

```bash
# 访问服务端公网 IP 的 18080 端口
curl http://YOUR_SERVER_IP:18080

# 或在浏览器打开
http://YOUR_SERVER_IP:18080
```

应该能看到本地 8080 端口的内容。

## ⚙️ 端口说明

### 控制通道端口（默认 17000，可配置）
- **作用**：用于客户端注册、心跳、状态同步等控制消息
- **服务端配置**：`server.addr`（默认 `:17000`，可通过配置修改）
- **客户端配置**：`client.server_addr`（必须与服务端 `server.addr` 一致）
- **说明**：控制端口可以修改，但客户端和服务端必须使用相同的端口

### 数据通道端口（完全可自定义）
- **作用**：用于实际业务流量的透明转发
- **客户端配置**：`client.remote_port`（可任意指定可用端口）
- **服务端**：自动监听 `client.remote_port` 指定的端口（接收用户 HTTP 请求）
- **转发**：将请求转发到客户端本地 `127.0.0.1:8080`
- **说明**：数据端口完全可自定义，只需确保端口未被占用且防火墙已开放

## 🔍 演示检查清单

- [ ] 本地 HTTP 服务正常运行（端口 8080）
- [ ] 服务端成功启动并监听 17000 端口（控制通道）
- [ ] 客户端成功连接服务端
- [ ] 端口映射注册成功（8080 -> 18080）
- [ ] 可以通过公网 IP:18080 访问本地服务
- [ ] 服务端防火墙已开放：
  - 17000（控制通道，客户端连接）
  - 18080（数据通道，用户访问）

## 🎬 演示技巧

1. **同时查看日志**：使用两个终端窗口，一个运行服务端，一个运行客户端
2. **展示健康检查**：
   - 停止本地 HTTP 服务，观察日志显示端口下线
   - 重新启动服务，观察日志显示端口上线
3. **展示心跳机制**：观察日志中的 ping/pong 消息
4. **切换日志语言**：修改 `log_lang` 为 "en" 查看英文日志

## ⚠️ 常见问题

### 问题1：无法访问 18080 端口

**原因**：服务端防火墙未开放端口

**解决**：
- 在云服务器安全组中开放 18080 端口
- 检查服务端防火墙规则：`sudo ufw allow 18080`

### 问题2：客户端连接失败

**原因**：服务端地址配置错误

**解决**：
- 检查 `server_addr` 是否正确
- 确保服务端监听 `0.0.0.0` 而不是 `127.0.0.1`
- 检查服务端防火墙是否开放 17000 端口

### 问题3：Token 认证失败

**原因**：客户端和服务端 token 不一致

**解决**：
- 确保 `client.token` 和 `server.token` 完全一致

## 📊 演示效果

演示成功后，你应该能够：

1. ✅ 通过公网 IP 访问本地 HTTP 服务
2. ✅ 看到清晰的日志输出（支持中英文）
3. ✅ 观察到健康检查和心跳机制的工作
4. ✅ 理解 gotunnel 的基本工作原理

